{
  "name": "daywork",
  "version": "1.0.0",
  "private": true,
  "description": "DayWork — full-stack job platform",
  "scripts": {
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.7.0"
  }
}
